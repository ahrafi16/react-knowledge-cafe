import PrpoTypes from 'prop-types';
const Blog = ({blog}) => {
    return (
        <div>
            
        </div>
    );
};

Blog.propTypes {
    blog : PrpoTypes.object.isRequired
}

export default Blog;